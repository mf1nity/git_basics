# git_basics
this file will contain a new repository that is being done in the command terminal and not on the actual git hub website

i first added the git package to my laptop
then i made a git directory called git
then i initialized that directory using the git init command
created the test_file.txt
used git add to add it to the staging section
then used git commit -m"" my message was the description of the text
then i typed git push to send it to github

i edited my readme.md file giving a general description of what it is 
also adding the new revision to the staging then commiting it
and finally pushing it to the actual github repository
